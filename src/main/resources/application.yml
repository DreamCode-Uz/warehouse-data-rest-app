spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/lesson2
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  data:
    rest:
      base-path: api
server:
  error:
    include-message: always
